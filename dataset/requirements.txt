# Dataset Generation Dependencies Only
# Training dependencies (Unsloth, PEFT, TRL) are in a separate environment

# Core
datasets>=2.14.0
numpy>=1.24.0
pandas>=2.0.0

# HuggingFace
transformers>=4.35.0
huggingface_hub>=0.19.0

# Utilities
tqdm>=4.66.0
jsonlines>=4.0.0
pyarrow>=14.0.0
